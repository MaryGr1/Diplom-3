Java 11, JUnit 4.13.2, maven 4.0.0, selenium-java 4.34.0

Проверяется: 

Регистрация (UserRegistredTest)

Успешную регистрацию.
Ошибку для некорректного пароля. Минимальный пароль — шесть символов.

Вход (UserLoginTest)

вход по кнопке «Войти в аккаунт» на главной;
вход через кнопку «Личный кабинет»;
вход через кнопку в форме регистрации;
вход через кнопку в форме восстановления пароля.

Раздел «Конструктор» (PointerTest)

Переходы к разделам:
«Булки»;
«Соусы»;
«Начинки».

По умолчанию тесты выполняются в браузере Chrome, для запуска тестов в Яндекс браузере:
В DriverFactory в методе startYandex указать путь до исполняемого файла Яндекс браузера;
Ввести в терминале: mvn clean test -Dbrowser=yandex

